
<!--
  This Behavior Tree replans the global path periodically at 1 Hz and it also has
  recovery actions specific to planning / control as well as general system issues.
  This will be continuous if a kinematically valid planner is selected.
-->
<!--
  @default_controller@ is replaced by navigation.launch.py with the
  'controller' launch argument (FollowPath or FollowPathGraceful).
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="@default_controller@" topic_name="controller_selector"/>
        <SmootherSelector selected_smoother="{selected_smoother}" default_smoother="savitzky_golay_smoother" topic_name="smoother_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <RateController hz="1.0">
          <Sequence>
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
            <SmoothPath unsmoothed_path="{path}" smoothed_path="{smooth_path}" smoother_id="{selected_smoother}" error_code_id="{smoother_error_code}"/>
          </Sequence>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{smooth_path}" controller_id="{selected_controller}" error_code_id="{follow_path_error_code}"/>
          <Sequence>
            <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence>
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldASmootherRecoveryHelp error_code="{smoother_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          <RoundRobin name="RecoveryActions">
            <Sequence name="ClearingActions">
              <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            <!--
              Upstream has a Spin here. It is dropped because on this scale
              it does harm and no good: rotating in place drifts, and a cube
              that leaves the play mat loses its Position ID, which stops the
              pose stream and needs a human to put it back (seen on hardware
              while reproducing toio_rmf_bringup#5). It buys nothing in
              exchange, because obstacle_layer has no observation sources on
              these robots, so turning reveals no new costmap information.

              BackUp is not used in its place either, although that is the
              motion that recovers the case this recovery subtree keeps
              failing to fix - a robot stopped inside a peer's footprint.
              Measured on hardware: the behavior refuses to run at all,
              because its collision check rejects the pose the robot is
              already standing in ("Collision Ahead - Exiting
              DriveOnHeading"), which is the very pose it would be escaping.
              That escape is done by toio_fleet_adapter instead, which drives
              cmd_vel directly and so is not blocked by the same check.
            -->
            <Wait wait_duration="5.0"/>
          </RoundRobin>
        </ReactiveFallback>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
